#pragma once

#include <unordered_map>

struct WorkspaceManager {   // note : not implemented yet

    WorkspaceManager();

    // void createWorkspace(const QSize&);

private:
    // std::unordered_map<std::string, >
};
